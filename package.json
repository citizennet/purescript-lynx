{
  "name": "purescript-formal",
  "scripts": {
    "build": "pulp build --to dist/main.js -- --censor-lib --stash --strict",
    "test": "pulp test"
  },
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.8.8",
    "pulp": "^12.3.1",
    "purescript": "^0.12.2",
    "purescript-psa": "^0.7.3"
  }
}
